<Window x:Class="TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Manager" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Panel: Filters and Search -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="120" Margin="0,0,10,0"
                      ItemsSource="{Binding StatusOptions}"
                      SelectedItem="{Binding SelectedStatus}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="Priority:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="120" Margin="0,0,10,0"
                      ItemsSource="{Binding PriorityOptions}"
                      SelectedItem="{Binding SelectedPriority}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="200" Margin="0,0,10,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Search" Width="80" 
                    Command="{Binding SearchCommand}"/>

            <Button Content="Clear" Width="80" Margin="10,0,0,0"
                    Command="{Binding ClearFiltersCommand}"/>

            <TextBlock Text="🔍 Filters Active" Margin="20,0,0,0" 
                       VerticalAlignment="Center" 
                       Foreground="Blue" FontWeight="Bold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasActiveFilters}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Task" Width="100" Margin="0,0,10,0"
                    Command="{Binding AddTaskCommand}"/>
            <Button Content="Edit Task" Width="100" Margin="0,0,10,0"
                    Command="{Binding EditTaskCommand}"/>
            <Button Content="Delete Task" Width="100" Margin="0,0,10,0"
                    Command="{Binding DeleteTaskCommand}"/>
            <Button Content="Refresh" Width="100" 
                    Command="{Binding LoadTasksCommand}"/>
        </StackPanel>

        <!-- Tasks DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="LightGray">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="80"/>
                <DataGridTextColumn Header="Assigned To" Binding="{Binding AssignedTo.Name}" Width="120"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="100"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedDateTime, StringFormat='{}{0:dd/MM/yyyy}'}" Width="100"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding EstimatedHours}" Width="60"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Height="25">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>